server:
  port: 3001
spring:
  application:
    name: eureka-client
  profiles:
    active: one
  cloud:
    config:
      uri: http://localhost:1001
management:
  endpoints:
    web:
      exposure:
        include: '*'
